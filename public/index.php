<?php

// TODO challenge => import the Composer autoloader

// TODO challenge => do something with each class contained in the `src` folder
// (hint: you can use the CLI or server mode)

// TODO challenge => display the name of a class using `::class`

// TODO challenge => implement a warehouse + container OOP logic using a different `Container` class 
// (hint: the new Container class must be in a different namespace)

// TODO boss-level challenge => CRUD connected to a MySQL database

// ! your program must not throw errors